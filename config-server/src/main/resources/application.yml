server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:/Users/andreeaionita/Developer/master/java facultate/proiect_facultate/config-repo
          clone-on-start: true
